import java.util.HashMap;
/**
 * Write a description of class ItemActions here.
 * (parralell of command words)
 * @author Andrew Riganati
 * @version 4.15.2019 
 */
public class ItemActions
{
    //a map between items and valid actions for those items.
    /**
    private HashMap<String, Items> validActions;
    /**
     * Constructor - Initialize item data.
     
    public ItemActions()
    {
        // initialise instance variables
        validActions = new HashMap<String, Items>();
        for(Items item : Items.values()){
            if( item != Items.THINGGUMMYWHAT){
                validActions.put(Items.toString(), item);
            }
        }
    }
    */
    /**
     *  public itemAction getItemAction(String commandWord)
    {
        //itemAction command
    }
     */
   
}
